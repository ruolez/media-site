# Development / plain-HTTP server. In production install.sh replaces this
# mount with a generated production.conf (HTTP->HTTPS redirect + TLS server)
# that includes the same app.conf snippet.

server {
    listen 80;
    server_name _;
    include /etc/nginx/snippets/app.conf;
}
